services:
  nginx:
    volumes:
      - ./nginx/https.conf.template:/etc/nginx/templates/default.conf.template:ro
      - ./certs:/etc/nginx/certs:ro
    ports:
      - "${RICSE_HTTPS_BIND:-127.0.0.1:8443}:443"
