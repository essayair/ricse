# Sot.md — Source of Truth

> 本项目所有关键决策的唯一真相入口。定稿后不可随意修改，变更须在"更新记录"中登记。
> 关联文档链：`Sot.md` ← `项目启动文档.md` ← `tech-architect.md` ← 各模块文档

---

## 项目标识

| 项 | 值 |
|---|-----|
| 产品/生态品牌 | **RICE** — Regional Industry Chain Ecosystem |
| 内部代号/仓库名 | **ricse** |
| 主域名 | ⚠️待确认 |
| 项目全称 | 区域产业链服务生态运营管理平台 |
| 当前阶段 | 一期线上测试版（合同至库存业务链已贯通，权限体系建设中） |

---

## 技术栈锁定

| 层次 | 选型 | 锁定状态 |
|------|------|---------|
| 架构模式 | 模块化单体 + DDD 有界上下文 | ✅ 已锁定 |
| 后端框架 | NestJS + TypeScript | ✅ 已锁定 |
| ORM | Prisma | ✅ 已锁定 |
| 主数据库 | PostgreSQL 15+ | ✅ 已锁定 |
| 缓存/队列 | Redis + BullMQ | ✅ 已锁定 |
| 文件存储 | MinIO（开发）/ 阿里云 OSS（生产） | ✅ 已锁定 |
| 前端框架 | Next.js 14+ (App Router) | ✅ 已锁定 |
| UI 库 | shadcn/ui + Tailwind CSS | ✅ 已锁定 |
| 服务端状态 | TanStack Query | ✅ 已锁定 |
| API 规范 | REST + OpenAPI (@nestjs/swagger) | ✅ 已锁定 |
| 认证一期 | 自建 JWT + Passport | ✅ 已锁定 |
| 部署 | Docker Compose（开发）/ 阿里云 ECS（生产） | ✅ 已锁定 |
| 项目组织 | pnpm Monorepo | ✅ 已锁定 |

> UI 库决策参考：`技术架构.md §3.4`（选 shadcn/ui 的理由）vs 项目启动文档 §2.2（选 Ant Design 5 的理由）。需评审最终锁定一个。

---

## 迭代策略

见 [docs/技术/迭代计划.md](./技术/迭代计划.md)。所有功能以**小步迭代**方式交付，每次迭代包含完整的 DB → API → 前端 → 测试链路。

## 一期当前范围

最初“仅合约主线”的验证目标已经完成，后续经产品决策扩展为线上测试业务链：

| 已纳入并完成第一版 ✅ | 后续建设 ⬜ |
|----------------------|-------------|
| 合同、角色会签顺序审批、执行批次、执行通知 | 流程版本、节点增删排序和通用审批中心 |
| 物流运单、磅单、质检单 | 设备直连、OCR 服务、承运商/质检机构门户 |
| 采购入库、销售出库、库存批次、库存台账、库存冲销 | 结算、发票、收付款完整闭环 |
| 自建 JWT、多角色 RBAC、用户角色数据范围 | 主数据与组织模块的外部门户精细化视图 |
| 外部企业合同及下游关联数据隔离 | 参与方映射和字段级敏感信息隔离 |

### 权限与数据范围锁定口径

- 功能权限与数据范围必须在同一项有效角色授权内绑定计算，不能跨角色拼接放大权限。
- 一个账号可拥有多个角色，数据范围配置在用户角色授权上。
- 外部企业账号固定查看所属企业合作伙伴作为卖方、买方或签约主体的合同及其全部下游关联数据，同企业员工共享，不限创建人。
- 外部企业不能查看无关企业数据、不能授予系统管理员、一期最多 6 个有效账号。
- 列表、详情、候选单据、状态操作和附件必须使用同一服务端范围。
- 结算、发票和收付款后续开发时继续继承合同或出入库来源范围。
- 平台统一使用“业务单元”承载大业务部的经营责任、利润中心属性、内部数据边界和审批责任；一个账号可归属多个业务单元，一份合同归属一个主业务单元。
- 唯一平台管理主体承载部门、员工、账号和业务单元；内部签约企业作为合作伙伴承担法律签约与库存货权。业务单元与签约主体独立组合，库存按“业务单元 + 货权主体 + 仓库 + 物料 + 批次”隔离。
- 多角色鉴权必须在同一角色授权内同时匹配目标权限与数据范围，禁止权限与其他角色范围交叉组合。
- 普通业务菜单按查看权限开放，新增、修改、删除、确认、审批、作废和过账等入口按操作权限隐藏，服务端继续强制校验。
- 外部企业账号试运行阶段仅开放必要业务权限，不开放主数据管理、组织管理及系统管理能力。

产品统一口径见[权限与数据范围规范](./产品/权限与数据范围规范.md)，技术实现见[角色权限与数据范围设计](./技术/模块设计/角色权限与数据范围.md)。
业务单元目标方案见[业务单元与权限审批优化方案](./产品/业务单元与权限审批优化方案.md)。

---

## 命名规范

### Git 提交前缀

| 前缀 | 场景 |
|------|------|
| `feat:` | 新功能 |
| `fix:` | 修复 |
| `docs:` | 仅文档 |
| `refactor:` | 重构 |
| `chore:` | 工程/配置 |
| `db:` | 数据库迁移/Prisma |
| `ai:` | AI 相关内容 |

### 分支命名

```
{type}/{描述}
示例：feat/contract-create, fix/login-redirect, db/add-contract-status
```

---

## 决策溯源

| 决策 | 来源 | 日期 |
|------|------|------|
| 模块化单体而非微服务 | 项目启动文档 v0.3 | 2026-07-01 |
| NestJS + TypeScript | 项目启动文档 v0.3 | 2026-07-01 |
| Prisma | 项目启动文档 v0.3 | 2026-07-01 |
| pnpm Monorepo | 项目启动文档 v0.3 | 2026-07-01 |
| PostgreSQL 15+ | 项目启动文档 v0.3 | 2026-07-01 |
| 数据库管理边界两条铁律 | 项目启动文档 v0.3 | 2026-07-01 |
| 多角色 + 用户角色数据范围 | 角色权限与数据范围设计 | 2026-07-27 |
| 外部企业共享本企业全部关联数据 | 权限与数据范围规范 | 2026-07-27 |
| 下游单据沿合同参与方继承企业范围 | 权限与数据范围规范 | 2026-07-27 |
| 业务单元承担经营责任、利润中心属性、内部数据隔离和审批选人边界 | 业务单元与权限审批优化方案 | 2026-09-15 |
| 平台管理主体、业务单元、法律/货权主体三维独立，签约主体不重复生成事业部 | 业务单元与权限审批优化方案 | 2026-09-15 |
| 一期不铺物流/库存/资金 | 项目启动文档 v0.3 | 2026-07-01 |
| 审批流一期写死不搞通用引擎 | 项目启动文档 v0.3 | 2026-07-01 |

---

## 更新记录

| 日期 | 变更人 | 变更内容 |
|------|--------|---------|
| 2026-07-01 | Claude | 初始创建，纳入项目启动文档 v0.3 关键决策 |
