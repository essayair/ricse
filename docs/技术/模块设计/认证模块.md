# 认证模块设计

> 版本：v1.0 | 日期：2026-07-02

## 概述

认证模块负责用户身份的注册、登录、鉴权和令牌管理。基于 JWT（JSON Web Token）实现无状态认证，配合 Refresh Token 机制提供安全的会话管理。

## API 端点

| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| POST | `/api/v1/auth/login` | ❌ | 用户登录，返回 accessToken + refreshToken |
| POST | `/api/v1/auth/register` | ❌ | 用户注册，自动返回 token |
| POST | `/api/v1/auth/refresh` | ❌ | 使用 refreshToken 刷新 accessToken |
| GET | `/api/v1/auth/profile` | ✅ JWT | 获取当前登录用户信息 |
| POST | `/api/v1/auth/logout` | ✅ JWT | 退出登录，清除 refreshToken |
| POST | `/api/v1/users` | ❌ | 创建用户（仅注册用，后续可限制） |

## 数据模型

### User 表（`users`）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String (cuid) | 主键 |
| username | String (unique) | 用户名 |
| password | String | bcrypt 哈希 |
| name | String | 显示名称 |
| role | String (USER/ADMIN/APPROVER) | 角色 |
| status | String (ACTIVE/DISABLED) | 状态 |
| refreshToken | String? | refreshToken 的 bcrypt 哈希 |
| createdAt/updatedAt | DateTime | 时间戳 |

## 认证流程

```
┌──────────┐     POST /auth/login     ┌──────────────┐
│  客户端   │ ──────────────────────→  │  认证服务     │
│          │ ←──────────────────────  │              │
└──────────┘    { accessToken,         └──────────────┘
                 refreshToken }              │
      │                                       │ bcrypt 比对
      │                                       ▼
      │                                  ┌──────────┐
      │  ┌────────────────────────────── │  User DB │
      │  │ Authorization: Bearer <jwt>    └──────────┘
      │  ▼
  ┌──────────┐     JwtStrategy          ┌──────────────┐
  │  请求    │ ──verify(secret)──────→  │   Passport   │
  └──────────┘                          └──────────────┘
```

## JWT 载荷

```typescript
// AccessToken
{
  sub: string;       // 用户 ID
  username: string;  // 用户名
  role: string;      // 角色
  iat: number;       // 签发时间
  exp: number;       // 过期时间
}

// RefreshToken
{
  sub: string;       // 用户 ID
  type: 'refresh';   // 类型标记
  iat: number;
  exp: number;
}
```

## 安全设计

### 密码存储
- 使用 `bcryptjs` 以 10 轮 salt 哈希存储
- `findByUsername()` 返回完整用户记录（含 password 哈希）供登录比对
- `findById()` 和 `profile()` 不暴露 password 字段

### 令牌安全
- AccessToken 有效期：24 小时（配置于 `JWT_EXPIRES_IN`）
- RefreshToken 有效期：7 天，用于无感刷新
- RefreshToken 以 bcrypt 哈希存储在 DB，即使 DB 泄露也无法直接使用
- 退出登录时清除 refreshToken，使旧 refreshToken 失效

### 401 处理（前端）
- `api.ts` 自动拦截 401 响应，清除 localStorage 并重定向到 `/login`

## 环境变量

```env
JWT_SECRET="ricse-dev-jwt-secret-2026"
JWT_REFRESH_SECRET="ricse-dev-refresh-secret-2026"
JWT_EXPIRES_IN="24h"
```

## 关键实现文件

| 文件 | 说明 |
|------|------|
| `src/modules/common/auth.service.ts` | 认证核心逻辑 |
| `src/modules/common/auth.controller.ts` | 认证 API 端点 |
| `src/modules/common/users.service.ts` | 用户 CRUD + refreshToken 管理 |
| `src/modules/common/jwt.strategy.ts` | Passport JWT 策略 |
| `src/modules/common/current-user.decorator.ts` | `@CurrentUser()` 参数装饰器 |
| `src/modules/common/dto/login.dto.ts` | 登录请求校验 |
| `src/modules/common/dto/register.dto.ts` | 注册请求校验 |
| `src/modules/common/dto/refresh.dto.ts` | 刷新令牌请求校验 |
