# 内容运营中心模块设计

> 版本：v1.0
> 日期：2026-08-20
> 状态：开发中

## 1. 现状与迁移来源

| 来源项目 | 当前能力 | 迁移结果 |
|---|---|---|
| `website-admin-dashboard` | 资讯、供需型资讯、官网合作咨询管理 | 改写为 RICSE Next.js 页面 |
| `hgchain-server` | 官网资讯、分类、价格、产品类型、咨询、萤石 Excel 接口 | 数据迁入 PostgreSQL，接口改写为 NestJS |
| `hgchain-server-17` | Java 17 升级分支及区域均价接口 | 不作为独立来源运行，仅补齐必要接口能力 |
| `fyzs-mini-app/server` | 资讯同步、AI 清洗、百川行情、AI 对话、微信登录、供需 JSON | API 和 Worker 迁入 RICSE |
| `sci99` | 已停用行情源 | 不启用，仅归档参考 |

旧若依框架的系统模块、Quartz 示例任务和无关业务模块不迁移。

## 2. 运行架构

代码统一在 RICSE Monorepo，运行时采用三个独立进程/容器：

```text
                         ┌─ core-api
                         │  合同、审批、物流、库存、生产
Nginx ───────────────────┤
                         ├─ content-api
                         │  内容后台、公开内容、行情、微信、实时 AI
                         │
                         └─ content-worker
                            资讯同步、批量 AI、行情采集、Excel 导入

PostgreSQL ─ 业务表 + 内容表（逻辑隔离）
Redis      ─ 缓存 + BullMQ 任务队列
MinIO      ─ ricse-attachments + content-assets
```

三进程可使用同一 Docker 镜像，通过不同启动命令加载不同 Nest Module。内容 API 和 Worker 使用独立数据库连接池，禁止直接修改合同、库存等核心表。

## 3. 当前外部任务清单

| 能力 | 当前触发方式 | 当前运行位置 | 目标位置 |
|---|---|---|---|
| 资讯同步与 AI 清洗 | 启动执行一次，之后每 30 分钟 | `fyzs-server` PM2 单进程 | `content-worker` |
| 百川行情下载/Excel 解析 | 用户查询、AI 工具调用触发，24 小时文件缓存 | `fyzs-server` PM2 单进程 | `content-worker` 定时采集 |
| 百川 Token 续期 | 查询时发现临期或 401 后触发 | `fyzs-server` PM2 单进程 | `content-worker` |
| DeepSeek 实时对话 | 用户请求 | `fyzs-server` PM2 单进程 | `content-api` |
| DeepSeek 批量摘要清洗 | 资讯同步过程 | `fyzs-server` PM2 单进程 | `content-worker` |
| 微信 `jscode2session` | 用户登录 | `fyzs-server` PM2 单进程 | `content-api` |
| 供需审核和公开查询 | 用户/后台请求，本地 JSON | `fyzs-server` PM2 单进程 | `content-api` + PostgreSQL |
| 萤石 Excel 载入 | Java 启动或手动刷新/上传 | `hgchain-server` Java 进程 | `content-worker` 导入任务 |
| 国际氢氟酸公开月度摘要 | 大屏直接嵌入 Google Looker Studio | 客户端浏览器，部分地区依赖 VPN | `content-worker` 每日采集并写入 PostgreSQL |
| 萤石区域价格趋势 | 大屏读取旧 Excel/接口 | 客户端或旧行情服务 | `content-worker` 每日采集 fluorspar.com 三地区年度接口 |

## 4. 数据模型

### 4.1 ContentCategory

- `id`：RICSE 内部 ID。
- `legacyId`：旧系统数字 ID，迁移和历史链接兼容使用。
- `name`、`code`、`sort`、`status`。

### 4.2 ContentArticle

- 基础：`title`、`summary`、`content`、`coverUrl`、`source`、`author`。
- 分类：`categoryId`、`tags`。
- 类型：`NEWS`、`SUPPLY`、`DEMAND`。
- 状态：`DRAFT`、`PUBLISHED`、`OFFLINE`。
- 供需扩展：商品、规格、数量、价格、地区、交货方式、要求、企业和联系人。
- 发布：`publishAt`、`publishedBy`、浏览量、点赞数。
- 兼容：`legacyId` 保留旧文章地址使用的数字 ID。

### 4.3 ProductType / ProductPrice

- 产品类型维护产品名称、编码、规格和状态。
- 价格记录业务日期、地区、市场、规格、价格、单位、来源及原始数据。
- 唯一约束覆盖“产品 + 市场/地区 + 规格 + 业务日期 + 来源”。

### 4.4 WebsiteContact

- 姓名、电话、企业、邮箱、留言、来源页面、状态、负责人和跟进备注。
- 公开创建接口限流；后台查询和处理必须鉴权。

### 4.5 SupplyDemandPost

- 供应/需求、商品、规格、数量、价格、地区和描述。
- 联系人、手机号、企业和微信 OpenID 仅后台或本人可见。
- 状态、驳回原因、来源、发布时间和审核人完整留痕。

### 4.6 ContentAsset

- 复用 RICSE `FileService` 与 MinIO 客户端，使用 `content/` 对象前缀。
- 记录对象键、原文件名、MIME、大小、用途和关联内容。
- 迁移时扫描并转存旧封面和正文内嵌媒体。

### 4.7 ContentJob

- 类型、业务唯一键、状态、参数、执行次数、下次重试时间。
- 开始/结束时间、成功数量、失败数量、错误摘要和日志引用。
- 支持幂等、人工重试、暂停和取消。

## 5. API 边界

### 5.1 运营后台

- `/api/v1/content/articles/**`
- `/api/v1/content/categories/**`
- `/api/v1/content/prices/**`
- `/api/v1/content/product-types/**`
- `/api/v1/content/contacts/**`
- `/api/v1/content/supply-demand/**`
- `/api/v1/content/data-sources/**`
- `/api/v1/content/jobs/**`

后台 API 使用 RICSE JWT、`PermissionGuard` 和内容权限字典。

### 5.2 公开接口

- `/api/v1/public/content/articles/**`
- `/api/v1/public/content/prices/**`
- `/api/v1/public/content/contacts`
- `/api/v1/public/content/supply-demand/**`
- `/api/v1/public/content/market/fluorite/regions`
- `/api/v1/public/content/market/fluorite/trend`
- `/api/v1/public/content/market/fluorite/price-trend`
- `/api/v1/public/content/market/hydrofluoric-acid/trend`
- `/api/v1/public/content/industry-data/FLUORITE_IMPORT_EXPORT`
- `/api/v1/public/ai/**`
- `/api/v1/public/wechat/login`

公开接口只返回已发布、到达发布时间且未下线的数据。联系方式、OpenID、内部状态和运营备注不允许进入公开响应。

### 5.3 统一接口边界

测试平台从 2026-08-22 起停止旧接口兼容。官网、小程序和数字大屏只允许调用 `/api/v1/**`；`api.hgyunlian.com` 将整个前缀交给 RICSE 网关，未知 `/app-api/**`、`/api/news/**`、`/api/market/**` 等旧路径直接返回 404，不再转发旧 Java 或小程序 NestJS 服务。

## 6. 任务执行与故障隔离

- Redis + BullMQ 承载可重试队列和周期任务。
- 资讯清洗默认并发 1；同一文章内容版本只处理一次。
- 百川同一数据源只允许一个采集任务运行，先写临时结果，完整校验后替换当前有效版本。
- Business Analytiq 采集不绕过付费下载：日常任务只读取页面明确披露的当月区域价格摘要；趋势初始化另使用互联网档案馆中可核验的 Business Analytiq 公开页面历史快照，当前基线为 2023-05、2024-12、2025-03、2025-09、2026-02。不得从趋势缩略图反算价格，也不得把涨跌幅推算点标记为真实历史。任务类型为 `HF_MARKET_SYNC`，默认每天 06:15 检查一次，同月同区域幂等更新。
- fluorspar.com 趋势任务类型为 `FLUORSPAR_TREND_SYNC`，默认每天 06:05 执行；区域映射固定为华中 `cn_vp`、华东 `eu_fv`、北方 `eu_vp`。同日重复点以后返回值覆盖前值，日涨跌绝对值达到 10% 时写入 `LARGE_CHANGE` 质量标记但不修改来源价格。
- 数字大屏通过自身同源 `/ricse-api/**` 代理访问 RICSE HTTPS API，禁止在浏览器端写死 HTTP IP；第三方页面和 Google 图表不直接嵌入大屏。
- 第三方请求必须设置连接和总超时；重试采用指数退避和随机抖动。
- 连续失败触发熔断；查询端降级到最近一次成功数据。
- 超过重试次数的任务进入失败队列，由内容运营人员人工重试。
- `content-worker` 设置 CPU/内存/并发上限，不能挤占 `core-api` 的保留资源。
- `content-worker` 同时加入仅用于第三方访问的 `egress` 网络；交易 API、PostgreSQL、Redis 和 MinIO 继续位于 `backend` 内部网络。禁止为了采集方便取消 `backend` 的内部网络隔离。

## 7. 数据迁移

1. 旧资讯已经迁入 `content_articles`，`LEGACY_NEWS` 数据源及其定时任务已删除，不再执行增量同步。
2. 产品价格和行情统一进入 `content_product_types`、`content_product_prices`，百川采集必须使用 RICSE Worker 的直连凭据，不允许回退旧 `3001` 服务。
3. 萤石进出口快照迁入 `content_industry_datasets`，公开代码为 `FLUORITE_IMPORT_EXPORT`。
4. 封面、正文图片和附件统一存储到 MinIO `content/` 前缀。
5. 历史数据保留 `legacyId` 仅用于追溯，不继续暴露旧路径。

迁移脚本必须幂等，重复执行不得生成重复内容。旧服务停止后若发现缺少数据，应通过 RICSE 导入任务补录，禁止恢复默认转发。

## 8. 上线与回滚

1. 测试环境同时运行 RICSE `core-api`、`content-api` 和 `content-worker`。
2. 官网、小程序、数字大屏全部使用 `/api/v1/**` 并完成回归测试。
3. Nginx 不保留旧接口路由别名，也不设置旧后端兜底。
4. 停止旧 Java `48080` 和小程序 NestJS `3001` 服务；旧数据库和代码仅作离线归档。
5. 异常通过 RICSE 日志、任务记录和新接口修复，不将流量切回旧服务。

## 9. 安全约束

- 原公开 Excel 上传和刷新入口必须收回为后台权限接口。
- 公开资讯详情必须校验已发布状态，禁止草稿泄漏。
- DeepSeek、微信、百川凭据只允许通过环境变量/密钥服务注入。
- 百川优先使用现有 Token；临期或收到 401/403 时，使用服务器密钥变量中的账号密码自动续期，真实凭据不得写入仓库。
- AI、登录、咨询和供需提交分别限流，避免滥用和费用失控。
- 富文本保存前和展示前执行白名单清洗，阻断 XSS。
- 日志不得记录微信密钥、DeepSeek Key、百川密码、完整 Token 或联系人隐私。
- 国际价格响应保留来源 URL、数据月份和采集时间；源站失败时不删除最近一次成功记录。
